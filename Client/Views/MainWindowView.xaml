<mah:MetroWindow x:Class="Client.MainWindowView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Client"
        xmlns:mah="clr-namespace:MahApps.Metro.Controls;assembly=MahApps.Metro"
        xmlns:vms="clr-namespace:Client.ViewModels"
        mc:Ignorable="d"
        Title="Client" Height="720" Width="1280">
    <mah:MetroWindow.DataContext>
        <vms:MainWindowViewModel/>
    </mah:MetroWindow.DataContext>
    <Grid>
        <StackPanel Orientation="Horizontal">
            <Button Width="100" Height="100" Command="{Binding ConnectToServerCommand}"/>
            <Button Width="100" Height="100" Command="{Binding DisconnectFromServerCommand}" Margin="50"/>
        </StackPanel>
    </Grid>
</mah:MetroWindow>
